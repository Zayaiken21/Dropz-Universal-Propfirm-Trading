services:
  - type: web
    name: propfirm-trading-platform
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: DATA_PROVIDER
        value: simulation
      - key: FINNHUB_API_KEY
        sync: false
